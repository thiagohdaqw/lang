exporte func escreva(texto):
    write(1, texto, strlen(texto))
fim

exporte func escreval(texto):
    escreva(texto)
    escreva("\n")
fim

exporte func escrevaf(formato, a, b, c, d, f):
    printf(formato, a, b, c, d, f)
fim